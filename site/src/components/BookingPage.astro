---
import { siteConfig } from '../data/siteConfig';
import PhoneIcon from './PhoneIcon.astro';
import WhatsAppIcon from './WhatsAppIcon.astro';

interface Props {
  lang: 'it' | 'en';
}

const { lang } = Astro.props;
const contacts = [
  {
    display: '+39 327 003 4324',
    phoneHref: 'tel:+393270034324',
    whatsappHref: 'https://wa.me/393270034324'
  },
  {
    display: '+39 324 863 2353',
    phoneHref: 'tel:+393248632353',
    whatsappHref: 'https://wa.me/393248632353'
  }
];

const content = {
  it: {
    title: 'PRENOTA IL TUO SOGGIORNO',
    intro: 'Siamo felici di aiutarti a organizzare il tuo soggiorno a Perla Toscana.<br />Contattaci telefonicamente o su WhatsApp per verificare la disponibilità e ricevere assistenza personalizzata.',
    callTitle: 'PRIMA DI PRENOTARE, CHIAMACI',
    callText: 'Siamo a tua disposizione per rispondere a ogni domanda<br />e aiutarti a scegliere le date migliori per il tuo soggiorno.',
    separator: 'oppure',
    whatsappTitle: 'SCRIVICI SU WHATSAPP',
    whatsappText: 'Puoi scriverci su uno dei nostri numeri WhatsApp:',
    onlineTitle: 'Verifica disponibilità online',
    onlineText: 'Puoi controllare le disponibilità in tempo reale e prenotare direttamente online in modo sicuro tramite il nostro sistema di prenotazione.',
    onlineButton: 'Verifica disponibilità online',
    phoneAria: 'Chiama Perla Toscana',
    whatsappAria: 'Scrivi a Perla Toscana su WhatsApp'
  },
  en: {
    title: 'BOOK YOUR STAY',
    intro: 'We are happy to help you organise your stay at Perla Toscana.<br />Contact us by phone or WhatsApp to check availability and receive personalised assistance.',
    callTitle: 'BEFORE BOOKING, CALL US',
    callText: 'We are available to answer your questions<br />and help you choose the best dates for your stay.',
    separator: 'or',
    whatsappTitle: 'MESSAGE US ON WHATSAPP',
    whatsappText: 'You can message us on either of our WhatsApp numbers:',
    onlineTitle: 'Check availability online',
    onlineText: 'You can check real-time availability and book securely online through our booking system.',
    onlineButton: 'Check availability online',
    phoneAria: 'Call Perla Toscana',
    whatsappAria: 'Message Perla Toscana on WhatsApp'
  }
};

const copy = content[lang];
---
<section class="booking-hero">
  <div class="container booking-hero__inner">
    <h1>{copy.title}</h1>
    <p set:html={copy.intro} />
  </div>
</section>

<section class="booking-contact">
  <div class="container booking-contact__inner">
    <article class="booking-method">
      <div class="booking-icon booking-icon--phone" aria-hidden="true">
        <PhoneIcon />
      </div>
      <h2>{copy.callTitle}</h2>
      <div class="booking-number-list" aria-label={copy.phoneAria}>
        {contacts.map((contact) => (
          <a class="booking-number" href={contact.phoneHref} aria-label={`${copy.phoneAria} ${contact.display}`}>
            {contact.display}
          </a>
        ))}
      </div>
      <p set:html={copy.callText} />
    </article>

    <div class="booking-separator" aria-hidden="true">
      <span>{copy.separator}</span>
    </div>

    <article class="booking-method">
      <div class="booking-icon booking-icon--whatsapp" aria-hidden="true">
        <WhatsAppIcon />
      </div>
      <h2>{copy.whatsappTitle}</h2>
      <p>{copy.whatsappText}</p>
      <div class="booking-whatsapp-list" aria-label={copy.whatsappAria}>
        {contacts.map((contact) => (
          <a class="booking-whatsapp-link" href={contact.whatsappHref} target="_blank" rel="noopener noreferrer" aria-label={`${copy.whatsappAria} ${contact.display}`}>
            <WhatsAppIcon />
            <span>{contact.display}</span>
          </a>
        ))}
      </div>
    </article>

    <article class="online-booking-box">
      <h2>{copy.onlineTitle}</h2>
      <p>{copy.onlineText}</p>
      <a class="button button--primary online-booking-button" href={siteConfig.placeholders.avaiBookUrl} target="_blank" rel="noopener noreferrer" data-track="availability_click">
        {copy.onlineButton}
      </a>
    </article>
  </div>
</section>

<style>
  .booking-hero {
    background: var(--color-sand);
    padding: clamp(58px, 9vw, 94px) 0 clamp(50px, 7vw, 78px);
    text-align: center;
  }

  .booking-hero__inner {
    display: grid;
    max-width: 760px;
    gap: 20px;
  }

  .booking-hero h1 {
    color: var(--color-olive);
    font-family: var(--font-serif);
    font-size: clamp(2.2rem, 5vw, 4rem);
    font-weight: 700;
    letter-spacing: 0;
    line-height: 0.98;
    margin: 0;
  }

  .booking-hero p {
    color: rgba(47, 42, 36, 0.78);
    font-size: clamp(1rem, 1.8vw, 1.16rem);
    line-height: 1.75;
    margin: 0;
  }

  .booking-contact {
    background: var(--color-warm-white);
    padding: clamp(48px, 8vw, 88px) 0;
  }

  .booking-contact__inner {
    display: grid;
    max-width: 760px;
    gap: 34px;
    text-align: center;
  }

  .booking-method {
    display: grid;
    justify-items: center;
    gap: 12px;
  }

  .booking-icon {
    display: inline-grid;
    width: 48px;
    height: 48px;
    place-items: center;
    border-radius: 999px;
    background: rgba(251, 244, 232, 0.9);
  }

  .booking-icon :global(svg) {
    width: 25px;
    height: 25px;
  }

  .booking-icon--phone {
    color: var(--color-terracotta);
  }

  .booking-icon--whatsapp {
    color: #2E7D4F;
  }

  .booking-method h2,
  .online-booking-box h2 {
    color: var(--color-olive);
    font-size: clamp(1rem, 2.2vw, 1.22rem);
    font-weight: 800;
    letter-spacing: 0.08em;
    line-height: 1.2;
    margin: 0;
    text-transform: uppercase;
  }

  .booking-number-list,
  .booking-whatsapp-list {
    display: grid;
    justify-items: center;
  }

  .booking-number-list {
    gap: 16px;
    margin: 4px 0;
  }

  .booking-number {
    color: var(--color-terracotta);
    font-family: var(--font-serif);
    font-size: clamp(2rem, 6vw, 3.35rem);
    font-weight: 700;
    line-height: 1;
    text-decoration: none;
    white-space: nowrap;
  }

  .booking-number:hover,
  .booking-number:focus-visible,
  .booking-whatsapp-link:hover span,
  .booking-whatsapp-link:focus-visible span {
    color: var(--color-terracotta-dark);
    outline: none;
  }

  .booking-whatsapp-list {
    gap: 12px;
    width: 100%;
    max-width: 430px;
    margin-top: 6px;
  }

  .booking-whatsapp-link {
    display: inline-flex;
    width: 100%;
    min-height: 54px;
    align-items: center;
    justify-content: center;
    gap: 10px;
    border: 1px solid rgba(46, 125, 79, 0.2);
    border-radius: 999px;
    background: rgba(46, 125, 79, 0.06);
    color: #2E7D4F;
    padding: 10px 18px;
    text-decoration: none;
    transition: background 0.18s ease, border-color 0.18s ease, color 0.18s ease;
  }

  .booking-whatsapp-link:hover,
  .booking-whatsapp-link:focus-visible {
    border-color: rgba(46, 125, 79, 0.34);
    background: rgba(46, 125, 79, 0.1);
    outline: none;
  }

  .booking-whatsapp-link :global(svg) {
    flex: 0 0 auto;
    width: 1.15em;
    height: 1.15em;
  }

  .booking-whatsapp-link span {
    color: var(--color-terracotta);
    font-family: var(--font-serif);
    font-size: clamp(1.65rem, 4.8vw, 2.35rem);
    font-weight: 700;
    line-height: 1;
    white-space: nowrap;
  }

  .booking-method p,
  .online-booking-box p {
    color: rgba(47, 42, 36, 0.72);
    line-height: 1.7;
    margin: 0;
  }

  .booking-separator {
    display: grid;
    align-items: center;
    grid-template-columns: 1fr auto 1fr;
    gap: 18px;
    color: rgba(47, 42, 36, 0.42);
    font-family: var(--font-serif);
    font-size: 1.1rem;
    font-style: italic;
  }

  .booking-separator::before,
  .booking-separator::after {
    content: '';
    height: 1px;
    background: rgba(184, 142, 97, 0.28);
  }

  .online-booking-box {
    display: grid;
    justify-items: center;
    gap: 14px;
    border: 1px solid rgba(184, 142, 97, 0.22);
    border-radius: 8px;
    background: rgba(251, 244, 232, 0.45);
    margin-top: 8px;
    padding: clamp(24px, 5vw, 38px);
  }

  .online-booking-box h2 {
    letter-spacing: 0;
    text-transform: none;
  }

  .online-booking-button {
    margin-top: 4px;
    border-radius: 999px;
  }

  @media (max-width: 560px) {
    .booking-hero {
      padding-top: 44px;
    }

    .booking-contact__inner {
      gap: 30px;
    }

    .booking-number {
      font-size: clamp(1.85rem, 9vw, 2.35rem);
    }

    .booking-number-list {
      gap: 14px;
    }

    .booking-whatsapp-link {
      min-height: 52px;
      padding-inline: 12px;
    }

    .booking-whatsapp-link span {
      font-size: clamp(1.45rem, 7.5vw, 1.85rem);
    }

    .online-booking-button {
      width: 100%;
      max-width: 320px;
    }
  }
</style>
