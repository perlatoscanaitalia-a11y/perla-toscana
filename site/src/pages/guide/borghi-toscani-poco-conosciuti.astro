---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageContent from '../../components/PageContent.astro';
import { hiddenVillagesIt as page } from '../../data/guideHiddenVillages';
import { faqSchema, guideSchema } from '../../data/schema';
const schema = [...guideSchema('it', page), faqSchema(page.faqs)].filter(Boolean);
---
<BaseLayout lang="it" title={page.title} description={page.description} path={page.path} alternatePaths={page.alternatePaths} schema={schema} ogType="article" socialImage={page.socialImage} socialImageAlt={page.socialImageAlt}>
  <PageContent lang="it" page={page} />
</BaseLayout>
